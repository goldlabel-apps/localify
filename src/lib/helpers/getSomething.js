// import { getStore } from '../../'

const getSomething = what => {

    switch ( what ) {
        case `en`:
            return `trfasas`
        default: {
            return false
        }
    
    }
}

export default getSomething
