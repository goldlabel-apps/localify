let primaryColor = `#ccd31f`
let secondaryColor = `#424242`

export const themeLight = { 
  palette: {
    type: 'light',
    background: {
      paper: '#fff',
      default: '#fff'
    },
    text:{
      primary: secondaryColor,
      secondary: '#ccd31f',
    },
    primary: {
      main: primaryColor,
    },
    secondary: {
      main: secondaryColor,
    },
  }
}
